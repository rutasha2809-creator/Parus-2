# Служебные папки, которые не должны попадать на GitHub
node_modules/
package.json
package-lock.json

# Отчёт последнего прогона тестов — нужен только локально
Тесты/результат.txt

# Системные файлы
.DS_Store
Thumbs.db
desktop.ini

# Служебная папка для файлов, которые не удалось удалить напрямую
_to_delete/
